# Examples

The `demo_build/` folder is generated by `tdc demo` and shows what the project produces without requiring any live-data download.

It contains:

- synthetic raw inputs
- processed quarterly estimates
- figures
- static-site-ready JSON and CSV outputs
